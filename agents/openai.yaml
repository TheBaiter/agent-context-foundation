interface:
  display_name: "Agent Context Foundation"
  short_description: "Compact routed agent docs, scripts, project maps, planning, and error memory"
  default_prompt: "Use $agent-context-foundation to inspect the repository, establish or reuse task traceability, preserve existing agent-instruction standards, create only the minimum viable routed context, and add evidence-backed maps, memory, tools, summaries, planning, rules, checklists, or customization only when they reduce future cost or risk."
