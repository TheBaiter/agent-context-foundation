interface:
  display_name: "Agent Context Foundation"
  short_description: "Modular routed context, verified memory, and task traceability"
  default_prompt: "Use $agent-context-foundation to inspect the repository's active agent instructions and task system, decide the minimum evidence-backed context change, load only the relevant module and template, apply every relevant skill rule directly rather than presenting it as advice, preserve canonical ownership and project-specific policies, keep disposable QA/visual artifacts in the repository's single temporary workspace, validate the result, and keep meaningful work traceable."
